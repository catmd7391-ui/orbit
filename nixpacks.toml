providers = ["python"]`n`n[phases.setup]`nnixPkgs = ["python310"]`n`n[phases.install]`ncmds = ["cd backend && pip install -r requirements.txt"]`n`n[start]`ncmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT}"
